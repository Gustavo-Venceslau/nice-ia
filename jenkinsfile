pipeline {
  agent any

  tools{
    maven 'Maven 3.9.2'
    jdk 'jdk8'
  }
  stages {

    stage('Build') {

      steps {
        sh 'mvn -Dmaven.test.failure.ignore=true install'
      }
      post {
        junit 'target/surefire-reports/**/*.xml'
      }
    }
  }
}
